import React from 'react';
import {shallow} from 'enzyme';
import Content from './content';
import handleInvite from './content';

describe('Content', () => {
    it('should be defined', () => {
        expect(Content).toBeDefined();
    });
});
